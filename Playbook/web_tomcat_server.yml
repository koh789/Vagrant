- hosts: '{{hosts}}'
  user: '{{user}}'
  sudo: yes
  tasks:
     - include: ./roles/common/tasks/setup.yml
     - include: ./roles/web/tasks/tomcat8.yml
     - name: start tomcat8
       action: command sudo nohup /usr/share/tomcat8/bin/catalina.sh start 
